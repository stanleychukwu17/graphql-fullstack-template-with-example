package middleware_test

import (
	"testing"
)

func TestDeserializerUser(t *testing.T) {
	// register user and login the user
	// controllers_test.MockTestRegisterAndLoginUser(t)

	// app, db, err := database.Setup()
	// if err != nil {
	// 	t.Fatalf("Could not set up the database and a new Fiber App: %v", err)
	// }

	// Create a test user
	// user := controllers_test.rgUserType{
	// 	User: models.User{
	// 		Name: "John Doe", Username: "johndoe", Email: "john@example.com", Password: "password", Gender: "male",
	// 	},
	// }
}
